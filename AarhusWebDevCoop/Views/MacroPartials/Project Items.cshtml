@inherits Umbraco.Web.Macros.PartialViewMacroPage

@{
	var selection = Model.Content.Site().FirstChild("projectsMain").Children()
						.Where(x => x.IsVisible());
}
<ul>
	@foreach(var item in selection){
		<li class="homepage-welcome">
			<a href="@item.Url">@item.Name</a>
		</li>
	}
</ul>

